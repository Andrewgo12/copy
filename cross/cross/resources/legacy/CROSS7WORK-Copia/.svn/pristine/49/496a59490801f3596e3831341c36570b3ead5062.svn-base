/**
* Propiedad intelectual de FullEngine
*
* Trae y muestra en alert los mensajes de error
* @author freina
* @date 02-Jul-2004 10:16
* @location Cali-Colombia
*/
function jsAccessKey(IdObject,HotKeyObject,ValueObject)
{
        var list='';
        list = document.getElementById(IdObject);

        if(list)
        {
                list.setAttribute('accessKey',HotKeyObject);
                if(list.type=="button")
                        list.value=ValueObject;
        }
}